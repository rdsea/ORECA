# Experiment

This directory contains the fault injector component of the framework. It is used to introduce various types of faults, such as network latency, packet loss, and resource contention, into the system to test the resilience and performance of the applications and the effectiveness of the RCA methods.