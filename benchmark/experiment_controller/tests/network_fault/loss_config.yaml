name: network-loss-preprocessing
duration: 300s
fault_type: NetworkFault.DELAY
target:
  namespace: default
  environment:
    - cloud
    - edge
  label_selectors:
    app: preprocessing
loss:
  loss: "10"
  correlation: "0.5"
