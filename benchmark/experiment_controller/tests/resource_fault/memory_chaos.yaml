apiVersion: chaos-mesh.org/v1alpha1
kind: StressChaos
metadata:
  name: memory-stress-example
spec:
  mode: all
  selector:
    labelSelectors:
      app: "inference-mobilenetv2"
  stressors:
    memory:
      workers: 1
      size: 600MB
