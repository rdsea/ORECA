name: resource-cpu-preprocessing
duration: 300s
fault_type: ResourceFault.CPU
target:
  namespace: default
  environment:
    - cloud
    - edge
  label_selectors:
    app: preprocessing
stress_cpu:
  workers: 1
  load: 100
