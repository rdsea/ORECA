name: resource-memory-preprocessing
duration: 300s
fault_type: ResourceFault.MEM
target:
  namespace: default
  environment:
    - cloud
    - edge
  label_selectors:
    app: preprocessing
stress_memory:
  workers: 1
  size: 150MB
