name: resource-io-preprocessing
duration: 300s
fault_type: ResourceFault.CPU
target:
  namespace: default
  environment:
    - cloud
    - edge
  label_selectors:
    app: preprocessing
io_chaos:
  action: delay
  path: /data
  percent: "100"
  delay: 500ms
  methods:
    - read
    - write
